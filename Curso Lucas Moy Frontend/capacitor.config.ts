import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'curso-lucas-moy',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
